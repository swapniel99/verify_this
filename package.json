{
  "name": "verify_this",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "scripts": {
    "build": "esbuild src/background.js src/sidepanel.js --bundle --outdir=. --format=iife",
    "watch": "esbuild src/background.js src/sidepanel.js --bundle --outdir=. --format=iife --watch"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {
    "@google/genai": "^1.50.1",
    "marked": "^18.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
